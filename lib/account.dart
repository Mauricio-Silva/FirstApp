import 'package:flutter/material.dart';

class XAccount extends StatelessWidget {
  const XAccount({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Account"),
      ),
    );
  }
}
